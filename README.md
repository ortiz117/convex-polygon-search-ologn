# PoligonosDF - Algoritmo O(log N) 🎯

Este proyecto implementa un algoritmo de geometría computacional en $O(\log N)$ para determinar si un punto se encuentra dentro, fuera o en la frontera de un polígono convexo.

## 🛠️ Tecnologías Utilizadas
* **Backend:** Java 21 + Spring Boot (Algoritmo de búsqueda binaria vectorial).
* **Frontend:** SvelteKit + Tailwind CSS (Interfaz gráfica interactiva).
* **Despliegue:** Docker & Docker Compose.

## 🚀 Cómo ejecutar el proyecto (One-Click Start)

Asegúrate de tener [Docker Desktop](https://www.docker.com/products/docker-desktop/) instalado y ejecutándose.

1. Abre una terminal en la raíz de este proyecto.
2. Ejecuta el siguiente comando para construir y levantar los contenedores:
   ```bash
   docker-compose up --build